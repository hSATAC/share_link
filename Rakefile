#TODO: custom install path

desc "Install executables"
task :install do |t|
  `cp bin/* /usr/local/bin/`
end

desc "Uninstall executables"
task :uninstall do |t|
  `rm /usr/local/bin/share_link`
  `rm /usr/local/bin/fetch_link`
end
